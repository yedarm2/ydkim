import { createServer } from 'asas';

import abc from 'abc';

const commonJS = require('commonModule');
const aModule = require('aModule');

createServer(1);
console.log(abc, commonJS, aModule);
console.log(number);
